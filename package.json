{
  "name": "weathercal-reborn",
  "version": "1.0.0",
  "private": true,
  "description": "Modular Scriptable Weather Cal widgets",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test tests/*.test.js",
    "check": "npm test && npm run build && node scripts/check.js"
  }
}
